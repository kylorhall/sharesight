import styled from 'styled-components'
import { Flex as baseFlex, Box as baseBox, Grid as baseGrid } from 'grid-styled'

export const Flex = styled(baseFlex).attrs({})`
  min-width: 100%;
`

export const Box = styled(baseBox).attrs({})``

export const Grid = styled(baseGrid).attrs({})``
